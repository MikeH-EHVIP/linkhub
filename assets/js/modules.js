/**
 * Tree of Links Module - Frontend JavaScript
 *
 * @package ElyseVIP\LinkHub
 */

(function($) {
    'use strict';
    
    // Module initialization (if needed for interactive features)
    $(document).ready(function() {
        // Add smooth scroll or animations if needed
        $('.lh-link, .lh-card').on('click', function(e) {
            // Track analytics or add custom behavior
            // Currently using server-side tracking via redirect
        });
    });
    
})(jQuery);
